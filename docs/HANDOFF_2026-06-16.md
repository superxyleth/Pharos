# Handoff - 2026-06-16

## Project

Project: `pharos-quant-strategy-lifecycle-skill`

Workspace:

```text
D:\Hackathons\0 PHAROS
```

GitHub:

```text
https://github.com/superxyleth/Pharos
```

Public endpoints:

```text
Health: http://150.158.28.155:3011/health
MCP:    http://150.158.28.155:3011/mcp
x402:   http://150.158.28.155:3011/x402/status
```

## Current Positioning

This is a Pharos Phase 1 MCP Skill submission for Agent workflows.

It should be judged as Agent Skill infrastructure, not as:

- a profitable trading strategy
- a live trading bot
- a transaction execution Agent
- a full x402 settlement service

Core value:

- reusable MCP Skill
- composable Agent workflow
- sandboxed strategy lifecycle
- long-history proxy market backtests
- Phase 2 artifact handoff
- safe-by-default Pharos readiness checks

## Latest Git State

Latest commit:

```text
bd47a0a chore: memory point 20260616-130510 - align runtime x402 phase 2 extension wording
```

Recent commits:

```text
bd47a0a align runtime x402 phase 2 extension wording
bbcadae position x402 as phase 2 extension and support resource method quotes
3c22aa1 polish judge tool count and market data source naming
b5527dc add formal judge report and review entrypoints
fe3eee3 prefer WBTC WETH long-history proxy strategy path
```

## Public Review Path

Recommended one-call MCP review path:

```text
quant_loop_run
symbol = WBTC
useOpenAI = false
```

Why WBTC:

- uses BTCUSDT Binance spot 1H candles as a three-year WBTC price proxy
- provides richer market evidence than short-history PROS data
- produces 7-period backtest summaries and reusable artifacts

Comparison path:

```text
symbol = WETH
```

## Market Data

Primary judge datasets:

```text
WBTC proxy: BTCUSDT Binance spot 1H
WETH proxy: ETHUSDT Binance spot 1H
Coverage:   2023-06-14T00:00:00Z to 2026-06-13T23:00:00Z
Candles:    26,304 per dataset
```

Dataset files:

```text
assets/market-data/binance/spot/1h/BTCUSDT_1H.binance_spot.last3y.csv
assets/market-data/binance/spot/1h/ETHUSDT_1H.binance_spot.last3y.csv
```

Important limitation:

```text
These are CEX spot proxy candles for WBTC/WETH research backtests.
They are not Pharos DEX pool candles, swap execution evidence, or on-chain settlement evidence.
```

## Tool Surface

Public MCP currently exposes 16 tools:

Core/research:

```text
pharos_network_status
pharos_wallet_info
strategy_preset_list
strategy_preset_get
strategy_generate
strategy_validate
strategy_backtest
strategy_backtest_matrix
strategy_advise
strategy_simulate
strategy_export_artifact
quant_loop_run
```

Optional x402:

```text
x402_payment_status
x402_product_catalog
x402_quote
x402_receipt_verify
```

## x402 State

x402 is now positioned as:

```text
Optional Phase 2 paid-access extension layer
```

Current public x402 mode is PHRS-only native receipt verification:

```text
Docs: https://docs.pharos.xyz/developer-guide/x402
Network: eip155:688689
Chain ID: 688689
Asset: PHRS
Settlement: server verifies an existing public-chain transfer receipt
Facilitator URL: not exposed in public quote responses
```

Current public paid-access mode after live test:

```text
Mode: PHRS native receipt verification
Live test tx: 0x196d9b9deb4aea43510245350977792cb7a0a43dac562e6b1e7adf952b57c28d
Explorer: https://atlantic.pharosscan.xyz/tx/0x196d9b9deb4aea43510245350977792cb7a0a43dac562e6b1e7adf952b57c28d
Route unlocked: POST /paid/quant-report
Payment response mode: native-phrs-receipt-verified
```

The public service does not require an ERC20 test token or a public facilitator URL. Payers send PHRS themselves and submit the confirmed txHash in `PAYMENT-SIGNATURE`.

It is not positioned as:

```text
Live trading
Production settlement
Transaction execution
Core Phase 1 requirement
```

Implemented behavior:

```text
GET  /x402/status
GET  /x402/products
POST /x402/quote
POST /x402/verify
GET  /paid/artifacts/:artifactId
POST /paid/quant-report
POST /paid/dry-run-plan
```

Important latest improvement:

```text
/x402/quote accepts either productId or resource + method.
Public quote responses advertise PHRS only and set facilitatorUrl to null.
```

Example:

```json
{
  "resource": "/paid/quant-report",
  "method": "POST"
}
```

Expected public result:

```text
product.id = paid-quant-report
httpStatus = 402
settlementBroadcastEnabled = false
onChainWritesEnabled = false
```

## Safety Boundary

Preserve these invariants:

```text
No live trading
No private key output
No transaction signing
No transaction broadcast
No swaps
No approvals
No transfers
No contract deployments
No on-chain writes
```

## Validation Snapshot

Latest confirmed validation:

```text
npm run validate:skill = passed, 115 checks
npm run typecheck = passed
npm test = passed
npm run judge:smoke = passed, 0 failed checks
```

Latest public strict judge smoke highlights:

```text
toolCount = 16
chainId = 688689
nativeToken = PHRS
x402 quote accepts resource + method
WBTC proxy marketEvidence = true
backtestPeriods = 7
benchmarkPeriods = 7
pharosIntegrationSummary.chainId = 688689
marketDataNativeToPharos = false
artifactId exists
codeHash exists
liveTradingEnabled = false
broadcastTransactions = false
onChainWrites = false
```

Latest public x402 strict smoke:

```text
npm run x402:smoke -- http://150.158.28.155:3011 --strict = passed
network = eip155:688689
activePaymentMode = native-phrs-receipt
activeAsset = PHRS
activeSettlement = server-verifies-existing-onchain-transfer
protected /paid/quant-report returns HTTP 402 without payment signature
```

Latest PHRS receipt live payment:

```text
0.01 PHRS sent from 0xE0af...Ae2F to 0x567E...4450
tx = 0x196d9b9deb4aea43510245350977792cb7a0a43dac562e6b1e7adf952b57c28d
paid route returned HTTP 200 with PAYMENT-RESPONSE header
```

Latest dependency audit:

```text
npm run audit:security = passed, found 0 vulnerabilities
ws is pinned to 8.21.0 through npm overrides for the affected dependency paths
```

Deployment caveat:

```text
The benchmark and Pharos chainContext improvements have been deployed to the public server.
npm run judge:smoke:strict passes against the public MCP runtime with 0 failed checks.
Public runtime is PHRS native receipt mode only, with no public localhost facilitator dependency and no ERC20 test-token requirement.
```

## Server State

Server:

```text
ubuntu@150.158.28.155
```

Server path:

```text
/opt/projects/pharos-quant-strategy-lifecycle-skill
```

Current observed process:

```text
npm run mcp
tsx src/server.ts
port 3011
```

Important deployment note:

```text
The server directory is not a Git repository.
git pull fails there.
Deploy by uploading explicit changed files over SFTP and restarting the runtime.
Do not bulk-delete files or directories.
```

## Important Files

Judge docs:

```text
docs/JUDGE_REPORT.md
docs/JUDGE_DEMO_60_SECONDS.md
docs/CURRENT_PROGRESS.md
docs/DEPENDENCY_AUDIT.md
docs/PHASE2_ARTIFACT_REUSE.md
```

MCP and x402 implementation:

```text
src/server.ts
src/tools/x402Tools.ts
src/x402/catalog.ts
src/x402/routes.ts
scripts/judge-smoke.mjs
```

Market data:

```text
assets/market-data/manifest.json
assets/market-data/binance/spot/1h/BTCUSDT_1H.binance_spot.last3y.csv
assets/market-data/binance/spot/1h/ETHUSDT_1H.binance_spot.last3y.csv
```

## Next Development Options

Best next improvements:

1. Add Pharos-native DEX/oracle/indexer market data when available.
2. Extend benchmark comparison outputs if deeper research diagnostics are useful.
3. Keep WBTC/WETH as the main judge path and PROS as optional project-token research.
4. Keep x402 framed as Phase 2 paid-access infrastructure, with all Phase 1 write/execution paths disabled.
5. Avoid feature creep before judging unless it improves review clarity or safety.

## Rules To Preserve

- Do not commit `.env`, `.ssh`, `.tools`, `.codex`, or `.gitconfig`.
- Use `memory-push.cmd "message"` for GitHub pushes.
- Do not bulk-delete files or directories.
- If deleting is ever required, delete only one explicit file path at a time.
- Do not weaken the Phase 1 safety boundary.

## New Thread Prompt

Use this when continuing in a new Codex thread:

```text
Current project is D:\Hackathons\0 PHAROS, the Pharos Quant Strategy Lifecycle Skill for DoraHacks Pharos Phase 1. Public health is http://150.158.28.155:3011/health and public MCP is http://150.158.28.155:3011/mcp. The project is positioned as reusable Agent Skill infrastructure, not a live trading bot. Main judge path is quant_loop_run with symbol=WBTC and useOpenAI=false, using BTCUSDT Binance spot 1H three-year candles as WBTC proxy data. WETH uses ETHUSDT three-year proxy data. Public MCP exposes 16 tools: 12 core/research and 4 optional x402 tools. x402 is framed as an optional Phase 2 paid-access extension layer; /x402/quote supports productId or resource+method. Public x402 mode is PHRS native receipt verification only: quote responses advertise PHRS, set facilitatorUrl to null, and verify a confirmed Pharos Atlantic txHash submitted in PAYMENT-SIGNATURE. A live 0.01 PHRS payment tx 0x196d9b9deb4aea43510245350977792cb7a0a43dac562e6b1e7adf952b57c28d unlocked POST /paid/quant-report with PAYMENT-RESPONSE mode native-phrs-receipt-verified. Core research remains free and no strategy execution/trading writes are enabled. Current deployed improvements add benchmark summaries, Pharos read-only integration summary, artifact chainContext, PHRS receipt verification, and npm audit cleanup. Latest commit is bd47a0a. Latest validation passed: validate:skill 115 checks, typecheck, npm test, judge:smoke, public judge:smoke:strict, public x402 smoke, PHRS live payment, and audit:security. Server path is /opt/projects/pharos-quant-strategy-lifecycle-skill and is not a Git repo; deploy by SFTP upload of explicit changed files and runtime restart. Do not commit .env/.ssh/.tools/.codex/.gitconfig. Do not bulk-delete files or directories. Push GitHub with memory-push.cmd only.
```
