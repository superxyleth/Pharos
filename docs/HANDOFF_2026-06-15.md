# Handoff - 2026-06-15

## Project

Project: `pharos-quant-strategy-lifecycle-skill`

Workspace:

```text
D:\Hackathons\0 PHAROS
```

GitHub:

```text
https://github.com/superxyleth/Pharos
```

Public endpoints:

```text
Health: http://150.158.28.155:3011/health
MCP:    http://150.158.28.155:3011/mcp
x402:   http://150.158.28.155:3011/x402/status
```

## Current Positioning

This is a Pharos Phase 1 Skill submission:

- Core Skill remains free, public, reusable, and reviewable.
- It exposes a Pharos-compatible MCP runtime for AI Agents.
- It provides quant strategy generation, validation, backtesting, advice, simulation, artifact export, and preset discovery.
- It does not perform live trading, transaction broadcast, swaps, approvals, transfers, deployments, or on-chain writes.

## Latest Git State

Latest commit:

```text
3dfa2f4 chore: memory point 20260615-194730 - address judge feedback: x402 phrs verification and safety summaries
```

Latest memory tag:

```text
memory/20260615-194730
```

Working tree status:

- `README.md`, `SKILL.md`, `references/mcp-tools.md`, `src/server.ts`, `src/strategy/backtest.ts`, `src/strategy/types.ts`, `src/tools/loopTools.ts`, `src/tools/strategyTools.ts` are modified.
- `assets/market-data/` is newly added.
- `scripts/build-pros-okx-klines.mjs`, `scripts/build-wphrs-real-klines.mjs`, `scripts/build-wphrs-slot0-klines.mjs`, `src/strategy/marketData.ts`, and `src/strategy/presets.ts` are newly added.

`origin/main` was previously synced; these changes are still local and not committed yet.

## Server State

Server:

```text
ubuntu@150.158.28.155
```

Server path:

```text
/opt/projects/pharos-quant-strategy-lifecycle-skill
```

Systemd service:

```text
pharos-quant-skill.service
```

Last known service status:

```text
active
```

Deployment status:

- The public server is running the previously deployed version.
- The OKX market-data loader, PROS DCA preset tools, and updated docs are local workspace changes and still need deployment.

Important deployment note:

The server directory is not a Git repository. `git pull` fails there with `fatal: not a git repository`.

Recent deployments used Python `paramiko` + SFTP to upload Git-tracked files, then ran:

```bash
npm install
npm run typecheck
sudo systemctl restart pharos-quant-skill.service
```

Do not bulk-delete server files.

## Core MCP Tools

The core research tools are now:

- `pharos_network_status`
- `pharos_wallet_info`
- `strategy_preset_list`
- `strategy_preset_get`
- `strategy_generate`
- `strategy_validate`
- `strategy_backtest`
- `strategy_backtest_matrix`
- `strategy_advise`
- `strategy_simulate`
- `strategy_export_artifact`
- `quant_loop_run`

Important behavior:

- `quant_loop_run` now prefers local market data for `PROS` when available.
- `strategy_preset_get` can return `pros-dca-guarded`.
- Backtests attach richer `dataQuality` fields, including dataset id/path, period, coverage note, and market evidence flags.

## Optional x402 Layer

An optional x402 paid gateway scaffold has been added.

MCP tools:

- `x402_payment_status`
- `x402_product_catalog`
- `x402_quote`
- `x402_receipt_verify`

HTTP routes:

- `GET /x402/status`
- `GET /x402/products`
- `POST /x402/quote`
- `POST /x402/verify`
- `GET /paid/artifacts/:artifactId`
- `POST /paid/quant-report`
- `POST /paid/dry-run-plan`

Current safety posture:

- `X402_ENABLED=false`
- no payment settlement
- no transaction signing
- no transaction broadcast
- no on-chain writes
- `/paid/*` routes return HTTP `402 Payment Required` by default
- `/mcp` core review path is not paywalled

## Market Data

Current local market datasets:

- OKX `PROS/USDT` 1H real market candles: `assets/market-data/okx/PROS_USDT_1H.okx.jsonl`
- Atlantic testnet WPHRS/USDT pool-state snapshots: `assets/market-data/atlantic-testnet/WPHRS_USDT_1H.slot0.real.jsonl`

Primary backtest dataset:

- `primaryMarketDatasetId = okx:PROS_USDT:1H`

Current OKX coverage:

- `PROS-USDT` history returned by OKX starts at `2026-05-13T10:00:00Z`
- Candle count: `796`
- The newest candle may have `confirm=false`

Important distinction:

- OKX `PROS/USDT` data is the real market source for strategy backtests.
- Atlantic WPHRS testnet data is only for on-chain pool-state reference and is not comparable to PROS/USD market evidence.

## Important Files

Core:

- `README.md`
- `SKILL.md`
- `src/server.ts`
- `src/config.ts`
- `src/tools/loopTools.ts`
- `src/tools/pharosTools.ts`
- `src/tools/strategyTools.ts`
- `src/strategy/marketData.ts`
- `src/strategy/presets.ts`

x402:

- `src/x402/catalog.ts`
- `src/x402/routes.ts`
- `src/tools/x402Tools.ts`
- `assets/x402-products.json`
- `references/x402-payments.md`
- `examples/x402-paid-artifact-flow.md`

Review and validation:

- `docs/DORAHACKS_SUBMISSION_SUMMARY.md`
- `references/evaluation-guide.md`
- `references/mcp-tools.md`
- `scripts/judge-smoke.mjs`
- `scripts/validate-skill.mjs`

Market data:

- `assets/market-data/manifest.json`
- `assets/market-data/okx/PROS_USDT_1H.okx.jsonl`
- `assets/market-data/atlantic-testnet/WPHRS_USDT_1H.slot0.real.jsonl`

## Latest Validation

Public validation:

```bash
npm run validate:skill
```

Result:

```text
115 checks passed
```

Offline validation:

```bash
npm run validate:skill -- --offline
```

Result:

```text
97 checks passed
```

Local project validation:

- `npm run typecheck` passed
- `npm test` passed
- `strategy_preset_get pros-dca-guarded` returned code in local MCP smoke

Public judge smoke:

```bash
npm run judge:smoke
```

Result:

```text
0 failed checks
toolCount = 14
```

HTTP x402 checks passed:

- `/x402/status`: `enabled=false`, `settlementBroadcastEnabled=false`, `onChainWritesEnabled=false`
- `/x402/products`: 3 paid products
- `/x402/quote`: returns quote with `httpStatus=402`
- `/paid/artifacts/pharos-demo`: returns HTTP `402 Payment Required`

## Next Development Options

1. Decide whether to expose `strategy_preset_list` and `strategy_preset_get` more prominently in README or the public reviewer flow.
2. Add an input flag or selector so `quant_loop_run` can explicitly choose between `PROS` OKX data and any future dataset.
3. Optionally add a `strategy_backtest_market` tool that takes an explicit dataset id.
4. Connect `/x402/verify` to a real x402 facilitator or official Pharos x402 sample.
5. Confirm whether x402 supports PHRS/native token on Pharos Atlantic, or choose a supported ERC20 asset.
6. Keep `/mcp`, `/health`, `tools/list`, and `quant_loop_run useOpenAI=false` free and stable for official review.

## Rules To Preserve

- Do not commit `.env`, `.ssh`, `.tools`, `.codex`, or `.gitconfig`.
- Push via `memory-push.cmd "message"`, not direct `git push`.
- Do not bulk-delete files or directories.
- Phase 1 boundary remains:
  - no live trading
  - no swaps
  - no approvals
  - no transfers
  - no contract deployments
  - no transaction broadcast
  - no on-chain writes
  - no private key output

## New Thread Prompt

Use this prompt when continuing in a new page:

```text
Current project is D:\Hackathons\0 PHAROS, the Pharos Quant Strategy Lifecycle Skill for DoraHacks Pharos Phase 1. Public MCP is http://150.158.28.155:3011/mcp and health is http://150.158.28.155:3011/health. The core Skill now includes preset discovery tools (`strategy_preset_list`, `strategy_preset_get`) plus the existing research tools. The active market data source is OKX PROS/USDT 1H candles stored at assets/market-data/okx/PROS_USDT_1H.okx.jsonl, and quant backtests prefer that dataset when symbol=PROS. Atlantic WPHRS testnet data is still present only as on-chain pool-state reference and is not comparable to PROS/USD market evidence. x402 still defaults to X402_ENABLED=false and does not settle payments, sign transactions, broadcast transactions, or write on-chain. Local validation currently passes typecheck, npm test, and validate:skill -- --offline. Server path is /opt/projects/pharos-quant-strategy-lifecycle-skill and is not a Git repo, so deploy with paramiko/SFTP file upload plus npm install, npm run typecheck, and sudo systemctl restart pharos-quant-skill.service. Push GitHub with memory-push.cmd only. Do not commit .env/.ssh/.tools/.codex/.gitconfig and do not bulk-delete files. Next work should decide whether to expose the preset tools more prominently, optionally add an explicit dataset selector for backtests, and keep the core /mcp review path free.
```
