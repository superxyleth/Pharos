# Handoff - 2026-06-15

## Project

Project: `pharos-quant-strategy-lifecycle-skill`

Workspace:

```text
D:\Hackathons\0 PHAROS
```

GitHub:

```text
https://github.com/superxyleth/Pharos
```

Public endpoints:

```text
Health: http://150.158.28.155:3011/health
MCP:    http://150.158.28.155:3011/mcp
x402:   http://150.158.28.155:3011/x402/status
```

## Current Positioning

This is a Pharos Phase 1 Skill submission:

- Core Skill remains free, public, reusable, and reviewable.
- It exposes a Pharos-compatible MCP runtime for AI Agents.
- It provides quant strategy generation, validation, backtesting, advice, simulation, and artifact export.
- It does not perform live trading, transaction broadcast, swaps, approvals, transfers, deployments, or on-chain writes.

## Latest Git State

Latest commit:

```text
0f9545c2941da0f185ae2778443e4f309454edc8
```

Latest memory tag:

```text
memory/20260615-154035-update-validation-count-after-x402-deplo
```

`origin/main` is synced. Local working tree was clean before this handoff document was added.

## Server State

Server:

```text
ubuntu@150.158.28.155
```

Server path:

```text
/opt/projects/pharos-quant-strategy-lifecycle-skill
```

Systemd service:

```text
pharos-quant-skill.service
```

Last known service status:

```text
active
```

Important deployment note:

The server directory is not a Git repository. `git pull` fails there with `fatal: not a git repository`.

Recent deployments used Python `paramiko` + SFTP to upload Git-tracked files, then ran:

```bash
npm install
npm run typecheck
sudo systemctl restart pharos-quant-skill.service
```

Do not bulk-delete server files.

## Core MCP Tools

The 10 core research tools are:

- `pharos_network_status`
- `pharos_wallet_info`
- `strategy_generate`
- `strategy_validate`
- `strategy_backtest`
- `strategy_backtest_matrix`
- `strategy_advise`
- `strategy_simulate`
- `strategy_export_artifact`
- `quant_loop_run`

## Optional x402 Layer

An optional x402 paid gateway scaffold has been added.

MCP tools:

- `x402_payment_status`
- `x402_product_catalog`
- `x402_quote`
- `x402_receipt_verify`

HTTP routes:

- `GET /x402/status`
- `GET /x402/products`
- `POST /x402/quote`
- `POST /x402/verify`
- `GET /paid/artifacts/:artifactId`
- `POST /paid/quant-report`
- `POST /paid/dry-run-plan`

Current safety posture:

- `X402_ENABLED=false`
- no payment settlement
- no transaction signing
- no transaction broadcast
- no on-chain writes
- `/paid/*` routes return HTTP `402 Payment Required` by default
- `/mcp` core review path is not paywalled

## Important Files

Core:

- `README.md`
- `SKILL.md`
- `src/server.ts`
- `src/config.ts`
- `src/tools/loopTools.ts`
- `src/tools/pharosTools.ts`
- `src/tools/strategyTools.ts`

x402:

- `src/x402/catalog.ts`
- `src/x402/routes.ts`
- `src/tools/x402Tools.ts`
- `assets/x402-products.json`
- `references/x402-payments.md`
- `examples/x402-paid-artifact-flow.md`

Review and validation:

- `docs/DORAHACKS_SUBMISSION_SUMMARY.md`
- `references/evaluation-guide.md`
- `references/mcp-tools.md`
- `scripts/judge-smoke.mjs`
- `scripts/validate-skill.mjs`

## Latest Validation

Public validation:

```bash
npm run validate:skill
```

Result:

```text
115 checks passed
```

Offline validation:

```bash
npm run validate:skill -- --offline
```

Result:

```text
97 checks passed
```

Public judge smoke:

```bash
npm run judge:smoke
```

Result:

```text
0 failed checks
toolCount = 14
```

HTTP x402 checks passed:

- `/x402/status`: `enabled=false`, `settlementBroadcastEnabled=false`, `onChainWritesEnabled=false`
- `/x402/products`: 3 paid products
- `/x402/quote`: returns quote with `httpStatus=402`
- `/paid/artifacts/pharos-demo`: returns HTTP `402 Payment Required`

## Next Development Options

1. Connect `/x402/verify` to a real x402 facilitator or official Pharos x402 sample.
2. Confirm whether x402 supports PHRS/native token on Pharos Atlantic, or choose a supported ERC20 asset.
3. Configure `X402_RECEIVER_ADDRESS` in `.env` only; do not commit it.
4. Implement real paid content for `/paid/quant-report`.
5. Implement real artifact lookup for `/paid/artifacts/:artifactId`.
6. Keep `/mcp`, `/health`, `tools/list`, and `quant_loop_run useOpenAI=false` free and stable for official review.

## Rules To Preserve

- Do not commit `.env`, `.ssh`, `.tools`, `.codex`, or `.gitconfig`.
- Push via `memory-push.cmd "message"`, not direct `git push`.
- Do not bulk-delete files or directories.
- Phase 1 boundary remains:
  - no live trading
  - no swaps
  - no approvals
  - no transfers
  - no contract deployments
  - no transaction broadcast
  - no on-chain writes
  - no private key output

## New Thread Prompt

Use this prompt when continuing in a new page:

```text
Current project is D:\Hackathons\0 PHAROS, the Pharos Quant Strategy Lifecycle Skill for DoraHacks Pharos Phase 1. Public MCP is http://150.158.28.155:3011/mcp and health is http://150.158.28.155:3011/health. The core Skill is complete and deployed with 10 research tools. An optional x402 paid gateway scaffold was added with 4 x402 MCP tools and /x402/* plus /paid/* HTTP routes. x402 defaults to X402_ENABLED=false and does not settle payments, sign transactions, broadcast transactions, or write on-chain. Latest public validate:skill passed 115 checks; judge:smoke passed with 0 failed checks and tools/list returns 14 tools. Server path is /opt/projects/pharos-quant-strategy-lifecycle-skill and is not a Git repo, so deploy with paramiko/SFTP file upload plus npm install, npm run typecheck, and sudo systemctl restart pharos-quant-skill.service. Push GitHub with memory-push.cmd only. Do not commit .env/.ssh/.tools/.codex/.gitconfig and do not bulk-delete files. Next x402 work should connect a real facilitator or receipt verification flow, confirm PHRS/native or supported ERC20 payment asset, and keep the core /mcp review path free.
```
